export default async function page() {
  // const sleep = (ms: number) => new Promise((resolve) => setTimeout(resolve, ms));

  // await sleep(3000);

  return (
    <div className="p-10">
      <h1>olovorgo</h1>
    </div>
  );
}
